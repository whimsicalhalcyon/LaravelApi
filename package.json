{
    "$schema": "https://www.schemastore.org/package.json",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "vite build",
        "dev": "vite",
        "dev-all": "npm run clear-build && npm run dev && cd frontend && npm run dev",
        "build-all": "npm run clear-build && npm run dev && cd frontend && npm run dev",
        "clear-build": "rm -rf public/build/*",
        "build:vue": "cd frontend && npm run build"
    },
    "devDependencies": {
        "@tailwindcss/vite": "^4.2.2",
        "axios": "^1.11.0",
        "concurrently": "^9.0.1",
        "laravel-vite-plugin": "^3.0.0",
        "tailwindcss": "^4.2.2",
        "vite": "^8.0.0"
    },
    "dependencies": {
        "@vitejs/plugin-vue": "^6.0.5",
        "vue-router": "^4.6.4"
    }
}
